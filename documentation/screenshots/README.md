# Screenshots

Images referenced by the root [`README.md`](../../README.md). Drop PNGs here with
these exact names and they'll render automatically:

| File | Shot |
|---|---|
| `login.png` | Landing / sign-in screen (logo lockup, typewriter, Google button, FAQ) |
| `folder.png` | A loaded folder — summary, file list with types/sizes, per-file checkboxes |
| `chat.png` | A chat answer showing inline `[n]` citations |
| `spend.png` | _(optional)_ Chat header with the `$X.XX / $5 used` indicator |

Capture from the running app at `http://localhost:5173`. Retina/2x is fine —
GitHub scales them down.
